# 青柠博客 · 安装教程

[English](./INSTALL-en.md)

一个轻量级个人博客系统，纯 PHP + MySQL 编写，无需 Composer，上传即可用。

---

## 一、环境要求

| 组件 | 要求 |
| --- | --- |
| PHP | 版本 ≥ 7.4（推荐 8.0+） |
| 数据库 | MySQL 5.7+ 或 MariaDB 10.3+ |
| PHP 扩展 | `pdo`、`pdo_mysql`、`mbstring`、`json`、`finfo`（必需）；`gd`（可选，用于验证码/图片） |
| 目录权限 | `uploads/` 目录需可写（用于图片上传） |

> 验证码功能依赖 `gd` 扩展，缺少时评论人机验证会无法显示，建议开启。

---

## 二、部署步骤

### 方式 A：虚拟主机 / Nginx / Apache（正式部署）

1. **上传源码**：把整个 `blog/` 目录里的文件上传到站点根目录（或某个子目录，如 `htdocs/blog`）。
   - 站点根目录指向 `blog/` 内部，即首页对应 `blog/index.php`。
2. **确认目录权限**：确保 `uploads/` 目录可写：
   ```bash
   chmod -R 755 uploads
   # 部分虚拟主机需要 775 或 777，视主机配置而定
   ```
3. **伪静态（可选）**：本系统 URL 均为 `xxx.php` 形式，默认无需伪静态即可运行。
   - Nginx 如使用 HTTPS，请确认 PHP-FPM 已正确转发。

### 方式 B：本地 PHP 内置服务器（快速体验）

```bash
cd blog
php -S 127.0.0.1:8080
```

浏览器访问 <http://127.0.0.1:8080> 即可进入安装向导。

---

## 三、运行安装向导

浏览器打开 `http://你的域名/install/index.php`，按 4 步完成：

### 第 1 步：环境检查
逐项核对版本与扩展，全部打勾（或仅 `gd` 未开启）即可点「下一步」。

### 第 2 步：数据库配置
填写 MySQL 连接信息：

| 字段 | 说明 |
| --- | --- |
| 数据库主机 | 默认 `127.0.0.1`，云数据库填对应地址 |
| 端口 | 默认 `3306` |
| 数据库名 | 只允许字母/数字/下划线，如 `blog`（不存在会自动创建） |
| 数据库用户 / 密码 | 有建库权限的账号 |

> 向导会自动创建数据库和全部数据表（`settings`、`posts`、`friends`、`comments`），无需手动建表。

### 第 3 步：站点与管理员
- **站点名称**：必填。
- **站点口号 / 副标题**：展示在首页 Hero 大标题处，可后改。
- **管理员用户名**：2-32 位，仅限字母、数字、下划线、中划线。
- **管理员密码**：至少 8 位，需输入两次。
- **安装目录**：
  - 根目录安装（域名直接指向站点）：**留空**；
  - 子目录安装（如 `http://域名/blog`）：填 `/blog`。

### 第 4 步：完成
看到「安装成功」即可访问首页，后台地址为 `http://你的域名/admin/`。

---

## 四、安装后的建议

1. **删除安装目录**（安全第一）：
   ```bash
   rm -rf install
   ```
   删除后安装向导无法再被访问，可防止他人重装/篡改配置。
2. **保管好 `config.php`**：其中包含数据库密码，请勿公开或提交到代码仓库。
3. 首次使用建议进入后台，把「首页设置」里的关于我（头像/姓名/文案）、公告等改成你自己的内容。

---

## 五、后台功能速览

后台地址：`/admin/`

| 菜单 | 功能 |
| --- | --- |
| 控制台 | 站点数据概览 |
| 文章管理 | 发布/编辑/删除文章（Markdown 写作） |
| 评论管理 | 审核 / 删除评论（新评论默认待审核） |
| 首页设置 | Hero、关于我（头像/姓名/文案）、最新文章数量等 |
| 站点设置 | 站点信息、ICP 备案、自定义 CSS 等 |
| 友情链接 | 管理友链 |

---

## 六、常见问题

**Q：安装时提示「数据库连接/初始化失败」？**
检查数据库主机、端口、用户名密码是否正确，且该账号有创建数据库的权限。

**Q：评论头像不显示？**
使用 QQ 邮箱评论会自动请求 QQ 头像接口；非 QQ 邮箱显示默认占位头像。请确认页面能访问外网。

**Q：验证码图片不显示？**
新版验证码已做降级处理：GD 可用时输出 PNG，GD 缺失/无 PNG 支持时自动改用 SVG 输出，任何环境都能出图。
若仍不显示，请检查：
- 浏览器是否拦截了该请求、页面与验证码是否同源；
- `captcha.php` 路径是否正确（可单独访问该地址看能否出图）；
- 服务器 PHP 是否开启了 `display_errors` 且上方有报错输出——可用开发者工具查看验证码请求的响应内容确认。

**Q：上传图片失败？**
`uploads/` 目录不可写，调整目录权限为 755/775/777 后重试。

**Q：想换数据库或重装？**
删除站点根目录的 `config.php` 后重新访问 `install/index.php` 即可（文章数据会保留，设置会重置）。

**Q：安装向导点「下一步 / 提交数据库」时 404？**
旧版本向导内部链接误用了 `install.php`，而实际文件是 `install/index.php`，导致相对链接解析到不存在的 `install.php` 而 404。
- 请确认 `install/index.php` 内容里已没有 `install.php` 字样（应全部为 `index.php`）；
- 若还是旧文件，重新上传修复后的 `install/index.php` 覆盖即可。

---

## 七、目录结构

```
blog/
├── index.php          首页（全屏翻页：Hero → 关于 → 最新文章 → 页脚）
├── about.php          关于我
├── blog.php           博客列表
├── post.php           文章详情（含评论系统 + 图片验证码）
├── friends.php        友情链接
├── captcha.php        评论验证码图片接口
├── admin/             后台管理
├── assets/            样式 / 脚本 / 图片
├── includes/          公共函数、配置、CSRF、验证码等
├── install/           安装向导（装完请删除）
└── uploads/           上传图片（需可写）
```

---

## 八、关于与开源

- 项目主页与特性介绍见 [README.md](./README.md)
- 开源协议：[MIT + 附加署名条款](./LICENSE)，可商用、可修改，但必须保留原作者署名
- 作者：takethe-cn；由 Trae AI（traeAI）辅助开发
